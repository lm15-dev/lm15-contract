---
meta:
  title: Chat completions schemas
  description: Full schema and model definitions referenced by the Chat completions API endpoint.
  keywords: chat completions, schemas, models, types, API reference, schema reference
cms:
  layout: large
  alias: /model-api/docs/api-reference/chat-completions/schemas
  target: aidmc
---

# Chat completions schemas

The full schema and model definitions referenced by the [Create a chat completion](/docs/api-reference/chat-completions/create-chat-completion) endpoint, including the request and response object shapes.

## Schemas

```openapi-schema
kind: schema
name: ChatCompletionMessageToolCall
schema:
  type: object
  title: Function tool call
  description: A function tool call emitted by the model.
  properties:
    id:
      type: string
      description: Identifier for this tool call.
    type:
      type: string
      enum:
      - function
      description: Tool type; always `function`.
    function:
      type: object
      description: The function the model chose to call.
      properties:
        name:
          type: string
          description: Name of the function to call.
        arguments:
          type: string
          description: Call arguments the model generated, as a JSON string. The model can emit invalid JSON or arguments outside your schema, so validate before invoking the function.
      required:
      - name
      - arguments
  required:
  - id
  - type
  - function
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCall: '#chat-completion-message-tool-call'
```
```openapi-schema
kind: schema
name: ChatCompletionMessageToolCallChunk
schema:
  type: object
  properties:
    index:
      type: integer
    id:
      type: string
      description: Identifier for this tool call.
    type:
      type: string
      enum:
      - function
      description: Tool type; always `function`.
    function:
      type: object
      properties:
        name:
          type: string
          description: Name of the function to call.
        arguments:
          type: string
          description: Function arguments generated by the model, as a JSON string. The model can produce invalid JSON or arguments outside your schema, so validate before executing.
  required:
  - index
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCallChunk: '#chat-completion-message-tool-call-chunk'
```
```openapi-schema
kind: schema
name: ChatCompletionMessageToolCalls
schema:
  type: array
  description: The tool calls the model generated, such as function calls.
  items:
    oneOf:
    - $ref: '#/components/schemas/ChatCompletionMessageToolCall'
    discriminator:
      propertyName: type
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCall: '#chat-completion-message-tool-call'
  ChatCompletionMessageToolCalls: '#chat-completion-message-tool-calls'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestAssistantMessage
schema:
  type: object
  title: Assistant message
  description: An assistant message holding a previous model response in the conversation.
  properties:
    content:
      anyOf:
      - oneOf:
        - type: string
          description: Assistant message content as a single text string.
          title: Text content
        - type: array
          description: 'Assistant message content as an array of parts: one or more `text` parts, or a single `refusal` part.'
          title: Array of content parts
          items:
            $ref: '#/components/schemas/ChatCompletionRequestAssistantMessageContentPart'
          minItems: 1
        description: Assistant message content. Required unless the message includes `tool_calls`.
      - type: 'null'
    refusal:
      anyOf:
      - type: string
        description: Refusal message issued by the assistant.
      - type: 'null'
    reasoning_content:
      anyOf:
      - type: string
        description: Reasoning (chain-of-thought) content the model produced on this assistant turn, replayed back so the model retains its prior reasoning across turns of a multi-turn conversation.
      - type: 'null'
    reasoning:
      anyOf:
      - type: string
        description: Alias of `reasoning_content` (Llama API convention). When both are present, `reasoning_content` takes precedence.
      - type: 'null'
    role:
      type: string
      enum:
      - assistant
      description: Role of the message author; always `assistant`.
    name:
      type: string
      description: Optional name for this participant, used to distinguish participants that share a role.
    tool_calls:
      $ref: '#/components/schemas/ChatCompletionMessageToolCalls'
  required:
  - role
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCall: '#chat-completion-message-tool-call'
  ChatCompletionMessageToolCalls: '#chat-completion-message-tool-calls'
  ChatCompletionRequestAssistantMessage: '#chat-completion-request-assistant-message'
  ChatCompletionRequestAssistantMessageContentPart: '#chat-completion-request-assistant-message-content-part'
  ChatCompletionRequestMessageContentPartRefusal: '#chat-completion-request-message-content-part-refusal'
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestAssistantMessageContentPart
schema:
  oneOf:
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartText'
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartRefusal'
  discriminator:
    propertyName: type
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestAssistantMessageContentPart: '#chat-completion-request-assistant-message-content-part'
  ChatCompletionRequestMessageContentPartRefusal: '#chat-completion-request-message-content-part-refusal'
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestDeveloperMessage
schema:
  type: object
  title: Developer message
  description: Developer-provided instructions that the model should follow, regardless of messages sent by the user. `developer` messages take precedence over `system` messages.
  properties:
    content:
      description: Developer message content.
      oneOf:
      - type: string
        description: Developer message content as a single text string.
        title: Text content
      - type: array
        description: Developer message content as an array of `text` parts.
        title: Array of content parts
        items:
          $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartText'
        minItems: 1
    role:
      type: string
      enum:
      - developer
      description: Role of the message author; always `developer`.
    name:
      type: string
      description: Optional name for this participant, used to distinguish participants that share a role.
  required:
  - content
  - role
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestDeveloperMessage: '#chat-completion-request-developer-message'
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestMessage
schema:
  oneOf:
  - $ref: '#/components/schemas/ChatCompletionRequestDeveloperMessage'
  - $ref: '#/components/schemas/ChatCompletionRequestSystemMessage'
  - $ref: '#/components/schemas/ChatCompletionRequestUserMessage'
  - $ref: '#/components/schemas/ChatCompletionRequestAssistantMessage'
  - $ref: '#/components/schemas/ChatCompletionRequestToolMessage'
  discriminator:
    propertyName: role
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCall: '#chat-completion-message-tool-call'
  ChatCompletionMessageToolCalls: '#chat-completion-message-tool-calls'
  ChatCompletionRequestAssistantMessage: '#chat-completion-request-assistant-message'
  ChatCompletionRequestAssistantMessageContentPart: '#chat-completion-request-assistant-message-content-part'
  ChatCompletionRequestDeveloperMessage: '#chat-completion-request-developer-message'
  ChatCompletionRequestMessage: '#chat-completion-request-message'
  ChatCompletionRequestMessageContentPartAudio: '#chat-completion-request-message-content-part-audio'
  ChatCompletionRequestMessageContentPartFile: '#chat-completion-request-message-content-part-file'
  ChatCompletionRequestMessageContentPartImage: '#chat-completion-request-message-content-part-image'
  ChatCompletionRequestMessageContentPartRefusal: '#chat-completion-request-message-content-part-refusal'
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
  ChatCompletionRequestMessageContentPartVideo: '#chat-completion-request-message-content-part-video'
  ChatCompletionRequestSystemMessage: '#chat-completion-request-system-message'
  ChatCompletionRequestSystemMessageContentPart: '#chat-completion-request-system-message-content-part'
  ChatCompletionRequestToolMessage: '#chat-completion-request-tool-message'
  ChatCompletionRequestToolMessageContentPart: '#chat-completion-request-tool-message-content-part'
  ChatCompletionRequestUserMessage: '#chat-completion-request-user-message'
  ChatCompletionRequestUserMessageContentPart: '#chat-completion-request-user-message-content-part'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestMessageContentPartAudio
schema:
  type: object
  title: Audio content part
  description: Audio input content for a message.
  properties:
    type:
      type: string
      enum:
      - input_audio
      description: The type of the content part. Always `input_audio`.
    input_audio:
      type: object
      properties:
        data:
          type: string
          description: Base64 encoded audio data.
        format:
          type: string
          enum:
          - wav
          - mp3
          description: 'The format of the encoded audio data. Currently supports "wav" and "mp3".

            '
      required:
      - data
      - format
    audio_url:
      type: string
      description: URL of the audio to send to the model. Fully qualified URL or base64-encoded data URL.
    file_id:
      type: string
      description: Identifier for the uploaded audio file to send to the model.
  required:
  - type
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartAudio: '#chat-completion-request-message-content-part-audio'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestMessageContentPartFile
schema:
  type: object
  title: File content part
  description: File input content for a message.
  properties:
    type:
      type: string
      enum:
      - file
      description: Content-part type; always `file`.
    file:
      type: object
      properties:
        filename:
          type: string
          description: Filename to associate with file data passed inline.
        file_data:
          type: string
          description: Base64-encoded file contents passed inline.
        file_id:
          type: string
          description: Identifier of an uploaded file to use as input.
  required:
  - type
  - file
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartFile: '#chat-completion-request-message-content-part-file'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestMessageContentPartImage
schema:
  type: object
  title: Image content part
  description: Image input content for a message.
  properties:
    type:
      type: string
      enum:
      - image_url
      description: Content-part type; always `image_url`.
    image_url:
      type: object
      properties:
        url:
          type: string
          description: Public URL or base64 data URL of the image.
          format: uri
        detail:
          type: string
          description: Specifies the detail level of the image.
          enum:
          - auto
          - low
          - high
          default: auto
      required:
      - url
  required:
  - type
  - image_url
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartImage: '#chat-completion-request-message-content-part-image'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestMessageContentPartRefusal
schema:
  type: object
  title: Refusal content part
  properties:
    type:
      type: string
      enum:
      - refusal
      description: Content-part type; always `refusal`.
    refusal:
      type: string
      description: Refusal message generated by the model.
  required:
  - type
  - refusal
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartRefusal: '#chat-completion-request-message-content-part-refusal'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestMessageContentPartText
schema:
  type: object
  title: Text content part
  description: Text input content for a message.
  properties:
    type:
      type: string
      enum:
      - text
      description: Content-part type; always `text`.
    text:
      type: string
      description: Text of this content part.
  required:
  - type
  - text
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestMessageContentPartVideo
schema:
  type: object
  title: Video content part
  description: A video input to the model. Provided as a URL or a base64 data URI.
  properties:
    type:
      type: string
      enum:
      - video_url
      description: The type of the content part.
    video_url:
      type: object
      properties:
        url:
          type: string
          description: Either a URL of the video or the base64 encoded video data.
          format: uri
        fps:
          anyOf:
          - type: number
            description: Frames per second at which to sample the video.
          - type: 'null'
      required:
      - url
  required:
  - type
  - video_url
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartVideo: '#chat-completion-request-message-content-part-video'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestSystemMessage
schema:
  type: object
  title: System message
  description: Instructions that the model should follow, regardless of messages sent by the user. `developer` messages take precedence over `system` messages.
  properties:
    content:
      description: System message content.
      oneOf:
      - type: string
        description: System message content as a single text string.
        title: Text content
      - type: array
        description: System message content as an array of `text` parts.
        title: Array of content parts
        items:
          $ref: '#/components/schemas/ChatCompletionRequestSystemMessageContentPart'
        minItems: 1
    role:
      type: string
      enum:
      - system
      description: Role of the message author; always `system`.
    name:
      type: string
      description: Optional name for this participant, used to distinguish participants that share a role.
  required:
  - content
  - role
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
  ChatCompletionRequestSystemMessage: '#chat-completion-request-system-message'
  ChatCompletionRequestSystemMessageContentPart: '#chat-completion-request-system-message-content-part'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestSystemMessageContentPart
schema:
  oneOf:
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartText'
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
  ChatCompletionRequestSystemMessageContentPart: '#chat-completion-request-system-message-content-part'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestToolMessage
schema:
  type: object
  title: Tool message
  properties:
    role:
      type: string
      enum:
      - tool
      description: Role of the message author; always `tool`.
    content:
      oneOf:
      - type: string
        description: Tool message content as a single text string.
        title: Text content
      - type: array
        description: Tool message content as an array of `text` parts.
        title: Array of content parts
        items:
          $ref: '#/components/schemas/ChatCompletionRequestToolMessageContentPart'
        minItems: 1
      description: Tool message content carrying the result of a tool call.
    tool_call_id:
      type: string
      description: Identifier of the tool call this message responds to.
  required:
  - role
  - content
  - tool_call_id
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
  ChatCompletionRequestToolMessage: '#chat-completion-request-tool-message'
  ChatCompletionRequestToolMessageContentPart: '#chat-completion-request-tool-message-content-part'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestToolMessageContentPart
schema:
  oneOf:
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartText'
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
  ChatCompletionRequestToolMessageContentPart: '#chat-completion-request-tool-message-content-part'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestUserMessage
schema:
  type: object
  title: User message
  description: A user message carrying end-user input such as a prompt or additional context.
  properties:
    content:
      description: User message content.
      oneOf:
      - type: string
        description: User message content as a single text string.
        title: Text content
      - type: array
        description: An array of content parts with a defined type. Supported options differ based on the model being used to generate the response. Can contain text, image, or audio inputs.
        title: Array of content parts
        items:
          $ref: '#/components/schemas/ChatCompletionRequestUserMessageContentPart'
        minItems: 1
    role:
      type: string
      enum:
      - user
      description: Role of the message author; always `user`.
    name:
      type: string
      description: Optional name for this participant, used to distinguish participants that share a role.
  required:
  - content
  - role
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartAudio: '#chat-completion-request-message-content-part-audio'
  ChatCompletionRequestMessageContentPartFile: '#chat-completion-request-message-content-part-file'
  ChatCompletionRequestMessageContentPartImage: '#chat-completion-request-message-content-part-image'
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
  ChatCompletionRequestMessageContentPartVideo: '#chat-completion-request-message-content-part-video'
  ChatCompletionRequestUserMessage: '#chat-completion-request-user-message'
  ChatCompletionRequestUserMessageContentPart: '#chat-completion-request-user-message-content-part'
```
```openapi-schema
kind: schema
name: ChatCompletionRequestUserMessageContentPart
schema:
  oneOf:
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartText'
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartImage'
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartAudio'
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartFile'
  - $ref: '#/components/schemas/ChatCompletionRequestMessageContentPartVideo'
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionRequestMessageContentPartAudio: '#chat-completion-request-message-content-part-audio'
  ChatCompletionRequestMessageContentPartFile: '#chat-completion-request-message-content-part-file'
  ChatCompletionRequestMessageContentPartImage: '#chat-completion-request-message-content-part-image'
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
  ChatCompletionRequestMessageContentPartVideo: '#chat-completion-request-message-content-part-video'
  ChatCompletionRequestUserMessageContentPart: '#chat-completion-request-user-message-content-part'
```
```openapi-schema
kind: schema
name: ChatCompletionResponseMessage
schema:
  type: object
  description: The message the model generated for a choice.
  properties:
    content:
      anyOf:
      - type: string
        description: Text content of the message.
      - type: 'null'
    refusal:
      anyOf:
      - type: string
        description: Refusal message generated by the model.
      - type: 'null'
    tool_calls:
      $ref: '#/components/schemas/ChatCompletionMessageToolCalls'
    annotations:
      type: array
      description: Annotations for the message, when applicable, as when using the web search tool.
      items:
        type: object
        description: A citation produced by the web search tool.
        required:
        - type
        - url_citation
        properties:
          type:
            type: string
            description: Annotation type; always `url_citation`.
            enum:
            - url_citation
          url_citation:
            type: object
            description: Details of a web source cited by the model.
            required:
            - end_index
            - start_index
            - url
            - title
            properties:
              end_index:
                type: integer
                description: Character offset in the message content where the cited span ends.
              start_index:
                type: integer
                description: Character offset in the message content where the cited span begins.
              url:
                type: string
                format: uri
                description: URL of the cited source.
              title:
                type: string
                description: Title of the cited source.
    reasoning_content:
      anyOf:
      - type: string
        description: The reasoning content generated by the model when using a reasoning model. This field does not carry the model's reasoning across turns; for multi-turn reasoning continuity, use the Responses API.
      - type: 'null'
    role:
      type: string
      enum:
      - assistant
      description: Role of the message author.
  required:
  - role
  - content
  - refusal
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCall: '#chat-completion-message-tool-call'
  ChatCompletionMessageToolCalls: '#chat-completion-message-tool-calls'
  ChatCompletionResponseMessage: '#chat-completion-response-message'
```
```openapi-schema
kind: schema
name: ChatCompletionStreamOptions
schema:
  anyOf:
  - description: Options that control streaming behavior. Set only when `stream` is `true`.
    type: object
    default: null
    properties:
      include_usage:
        type: boolean
        description: 'When set, a final chunk is emitted before `data: [DONE]` carrying `usage` for the whole request, with an empty `choices` array; earlier chunks report a null `usage`. An interrupted stream may not deliver this chunk.'
      include_obfuscation:
        type: boolean
        description: When true, stream obfuscation will be enabled. Stream obfuscation adds random characters to an `obfuscation` field on streaming delta events to normalize payload sizes as a mitigation to certain side-channel attacks. These obfuscation fields are included by default, but add a small amount of overhead to the data stream. You can set `include_obfuscation` to false to optimize for bandwidth if you trust the network links between your application and the API.
  - type: 'null'
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionStreamOptions: '#chat-completion-stream-options'
```
```openapi-schema
kind: schema
name: ChatCompletionStreamResponseDelta
schema:
  type: object
  description: An incremental message delta from a streamed completion.
  properties:
    content:
      anyOf:
      - type: string
        description: Text carried in this streamed delta.
      - type: 'null'
    reasoning_content:
      anyOf:
      - type: string
        description: The reasoning content chunk generated by the model when using a reasoning model. This field does not carry the model's reasoning across turns; for multi-turn reasoning continuity, use the Responses API.
      - type: 'null'
    tool_calls:
      type: array
      items:
        $ref: '#/components/schemas/ChatCompletionMessageToolCallChunk'
    role:
      type: string
      enum:
      - developer
      - system
      - user
      - assistant
      - tool
      description: Role of the message author.
    refusal:
      anyOf:
      - type: string
        description: Refusal text carried in this streamed delta.
      - type: 'null'
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCallChunk: '#chat-completion-message-tool-call-chunk'
  ChatCompletionStreamResponseDelta: '#chat-completion-stream-response-delta'
```
```openapi-schema
kind: schema
name: ChatCompletionTokenLogprob
schema:
  type: object
  properties:
    token:
      description: A single text token.
      type: string
    logprob:
      description: Log probability of this token. Tokens outside the top 20 report `-9999.0`.
      type: number
    bytes:
      anyOf:
      - description: UTF-8 byte values for this token, used to reconstruct text when a character spans multiple tokens. `null` when no byte representation exists.
        type: array
        items:
          type: integer
      - type: 'null'
    top_logprobs:
      description: List of the most likely tokens and their log probability, at this token position. In rare cases, there may be fewer than the number of requested `top_logprobs` returned.
      type: array
      items:
        type: object
        properties:
          token:
            description: A candidate text token.
            type: string
          logprob:
            description: Log probability of this token. Tokens outside the top 20 report `-9999.0`.
            type: number
          bytes:
            anyOf:
            - description: UTF-8 byte values for this token, used to reconstruct text when a character spans multiple tokens. `null` when no byte representation exists.
              type: array
              items:
                type: integer
            - type: 'null'
        required:
        - token
        - logprob
        - bytes
  required:
  - token
  - logprob
  - bytes
  - top_logprobs
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionTokenLogprob: '#chat-completion-token-logprob'
```
```openapi-schema
kind: schema
name: ChatCompletionTool
schema:
  type: object
  title: Function tool
  description: A function the model can call.
  properties:
    type:
      type: string
      enum:
      - function
      description: Tool type; always `function`.
    function:
      $ref: '#/components/schemas/FunctionObject'
  required:
  - type
  - function
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionTool: '#chat-completion-tool'
  FunctionObject: '#function-object'
  FunctionParameters: '#function-parameters'
```
```openapi-schema
kind: schema
name: ChatCompletionToolChoiceOption
schema:
  description: '`auto` lets the model choose between replying with a message and making one or more tool calls. Defaults to `auto` when tools are supplied.'
  oneOf:
  - type: string
    title: Tool choice mode
    description: '`auto` lets the model choose between replying with a message and making one or more tool calls.'
    enum:
    - auto
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionToolChoiceOption: '#chat-completion-tool-choice-option'
```
```openapi-schema
kind: schema
name: CompletionUsage
schema:
  type: object
  description: Token usage for the request.
  properties:
    completion_tokens:
      type: integer
      default: 0
      description: Tokens in the generated completion.
    prompt_tokens:
      type: integer
      default: 0
      description: Tokens in the prompt.
    total_tokens:
      type: integer
      default: 0
      description: Total tokens used, prompt plus completion.
    completion_tokens_details:
      type: object
      description: Breakdown of the completion tokens.
      properties:
        reasoning_tokens:
          type: integer
          default: 0
          description: Tokens the model spent on reasoning.
    prompt_tokens_details:
      type: object
      description: Breakdown of the prompt tokens.
      properties:
        cached_tokens:
          type: integer
          default: 0
          description: Prompt tokens served from cache.
  required:
  - prompt_tokens
  - completion_tokens
  - total_tokens
components:
  schemas: {}
page_linked_schemas:
  CompletionUsage: '#completion-usage'
```
```openapi-schema
kind: schema
name: CreateChatCompletionRequest
schema:
  allOf:
  - $ref: '#/components/schemas/CreateModelResponseProperties'
  - type: object
    properties:
      messages:
        description: An ordered list of the messages in the conversation. Each message has a `role` and `content`, and the model reads them in sequence to produce its reply.
        type: array
        minItems: 1
        items:
          $ref: '#/components/schemas/ChatCompletionRequestMessage'
      model:
        description: ID of the model used to generate the response.
        type: string
      modalities:
        $ref: '#/components/schemas/ResponseModalities'
      reasoning_effort:
        $ref: '#/components/schemas/ReasoningEffort'
      max_completion_tokens:
        description: Upper bound on the tokens the model may generate for a completion, counting both visible output and reasoning tokens.
        type: integer
        nullable: true
      frequency_penalty:
        type: number
        default: 0
        minimum: -2
        maximum: 2
        nullable: true
        description: Penalizes tokens in proportion to how often they have already appeared, reducing verbatim repetition. Ranges from -2.0 to 2.0; positive values discourage repetition.
      presence_penalty:
        type: number
        default: 0
        minimum: -2
        maximum: 2
        nullable: true
        description: Penalizes tokens that have already appeared at all, nudging the model toward new topics. Ranges from -2.0 to 2.0; positive values increase that push.
      top_logprobs:
        description: 'An integer between 0 and 20 giving the number of top alternative tokens to return at each position, each with its log probability. Requires `logprobs: true`.'
        type: integer
        minimum: 0
        maximum: 20
        nullable: true
      response_format:
        description: 'Constrains the format of the model''s output. Use `{ "type": "json_schema", ... }` for Structured Outputs, which forces the response to match your JSON schema, or `{ "type": "json_object" }` for the older JSON mode, which only guarantees valid JSON. Prefer `json_schema`.'
        oneOf:
        - $ref: '#/components/schemas/ResponseFormatText'
        - $ref: '#/components/schemas/ResponseFormatJsonSchema'
        - $ref: '#/components/schemas/ResponseFormatJsonObject'
        discriminator:
          propertyName: type
      store:
        type: boolean
        default: false
        nullable: true
        description: 'Whether or not to store the output of this chat completion request for use in our model distillation or evals products. Supports text and image inputs. Note: image inputs over 8MB will be dropped.'
      stream:
        description: When `true`, the response is streamed incrementally as [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format) rather than returned in a single payload.
        type: boolean
        nullable: true
        default: false
      stop:
        $ref: '#/components/schemas/StopConfiguration'
      logprobs:
        description: When `true`, returns the log probability of each token in the message `content`.
        type: boolean
        default: false
        nullable: true
      max_tokens:
        description: The maximum number of tokens that can be generated in the chat completion. Deprecated in favor of `max_completion_tokens`.
        type: integer
        nullable: true
        deprecated: true
      n:
        type: integer
        example: 1
        minimum: 1
        maximum: 128
        default: 1
        nullable: true
        description: Number of completions to generate for the input. Only `n=1` is supported.
      seed:
        type: integer
        minimum: -9223372036854776000
        maximum: 9223372036854776000
        nullable: true
        deprecated: true
        description: If set, the system makes a best-effort attempt to sample deterministically, so repeated requests with the same `seed` and parameters return similar results. Determinism is not guaranteed.
      stream_options:
        $ref: '#/components/schemas/ChatCompletionStreamOptions'
      tools:
        type: array
        description: A list of tools the model may call. You can provide either custom tools or function tools.
        items:
          oneOf:
          - $ref: '#/components/schemas/ChatCompletionTool'
      tool_choice:
        $ref: '#/components/schemas/ChatCompletionToolChoiceOption'
      parallel_tool_calls:
        $ref: '#/components/schemas/ParallelToolCalls'
    required:
    - model
    - messages
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCall: '#chat-completion-message-tool-call'
  ChatCompletionMessageToolCalls: '#chat-completion-message-tool-calls'
  ChatCompletionRequestAssistantMessage: '#chat-completion-request-assistant-message'
  ChatCompletionRequestAssistantMessageContentPart: '#chat-completion-request-assistant-message-content-part'
  ChatCompletionRequestDeveloperMessage: '#chat-completion-request-developer-message'
  ChatCompletionRequestMessage: '#chat-completion-request-message'
  ChatCompletionRequestMessageContentPartAudio: '#chat-completion-request-message-content-part-audio'
  ChatCompletionRequestMessageContentPartFile: '#chat-completion-request-message-content-part-file'
  ChatCompletionRequestMessageContentPartImage: '#chat-completion-request-message-content-part-image'
  ChatCompletionRequestMessageContentPartRefusal: '#chat-completion-request-message-content-part-refusal'
  ChatCompletionRequestMessageContentPartText: '#chat-completion-request-message-content-part-text'
  ChatCompletionRequestMessageContentPartVideo: '#chat-completion-request-message-content-part-video'
  ChatCompletionRequestSystemMessage: '#chat-completion-request-system-message'
  ChatCompletionRequestSystemMessageContentPart: '#chat-completion-request-system-message-content-part'
  ChatCompletionRequestToolMessage: '#chat-completion-request-tool-message'
  ChatCompletionRequestToolMessageContentPart: '#chat-completion-request-tool-message-content-part'
  ChatCompletionRequestUserMessage: '#chat-completion-request-user-message'
  ChatCompletionRequestUserMessageContentPart: '#chat-completion-request-user-message-content-part'
  ChatCompletionStreamOptions: '#chat-completion-stream-options'
  ChatCompletionTool: '#chat-completion-tool'
  ChatCompletionToolChoiceOption: '#chat-completion-tool-choice-option'
  CreateChatCompletionRequest: '#create-chat-completion-request'
  CreateModelResponseProperties: '#create-model-response-properties'
  FunctionObject: '#function-object'
  FunctionParameters: '#function-parameters'
  Metadata: '#metadata'
  ModelResponseProperties: '#model-response-properties'
  ParallelToolCalls: '#parallel-tool-calls'
  ReasoningEffort: '#reasoning-effort'
  ResponseFormatJsonObject: '#response-format-json-object'
  ResponseFormatJsonSchema: '#response-format-json-schema'
  ResponseFormatJsonSchemaSchema: '#response-format-json-schema-schema'
  ResponseFormatText: '#response-format-text'
  ResponseModalities: '#response-modalities'
  ServiceTier: '#service-tier'
  StopConfiguration: '#stop-configuration'
```
```openapi-schema
kind: schema
name: CreateChatCompletionResponse
schema:
  type: object
  description: A chat completion produced by the model for a given request.
  properties:
    id:
      type: string
      description: Identifier for this chat completion.
    choices:
      type: array
      description: List of completion choices. Only one choice is returned; `n` greater than 1 returns `HTTP 400`.
      items:
        type: object
        required:
        - finish_reason
        - index
        - message
        - logprobs
        properties:
          finish_reason:
            type: string
            description: 'Why the model stopped generating tokens: `stop` for a natural end, `length` when the output-token limit was reached, and `tool_calls` when the model requested a tool. `content_filter` and the deprecated `function_call` may also appear.'
            enum:
            - stop
            - length
            - tool_calls
            - content_filter
            - function_call
          index:
            type: integer
            description: Index of this choice in the `choices` array.
          message:
            $ref: '#/components/schemas/ChatCompletionResponseMessage'
          logprobs:
            anyOf:
            - description: Log-probability data for this choice.
              type: object
              properties:
                content:
                  anyOf:
                  - description: Content tokens with their log probabilities.
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatCompletionTokenLogprob'
                  - type: 'null'
                refusal:
                  anyOf:
                  - description: Refusal tokens with their log probabilities.
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatCompletionTokenLogprob'
                  - type: 'null'
              required:
              - content
              - refusal
            - type: 'null'
    created:
      type: integer
      format: unixtime
      description: Unix timestamp, in seconds, when the chat completion was created.
    model:
      type: string
      description: ID of the model that generated the completion.
    service_tier:
      $ref: '#/components/schemas/ServiceTier'
    system_fingerprint:
      type: string
      deprecated: true
      description: Identifies the backend configuration the model ran with, for use with `seed` to detect determinism-affecting changes. Not currently populated.
    object:
      type: string
      description: Object type; always `chat.completion`.
      enum:
      - chat.completion
    usage:
      $ref: '#/components/schemas/CompletionUsage'
  required:
  - choices
  - created
  - id
  - model
  - object
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCall: '#chat-completion-message-tool-call'
  ChatCompletionMessageToolCalls: '#chat-completion-message-tool-calls'
  ChatCompletionResponseMessage: '#chat-completion-response-message'
  ChatCompletionTokenLogprob: '#chat-completion-token-logprob'
  CompletionUsage: '#completion-usage'
  CreateChatCompletionResponse: '#create-chat-completion-response'
  ServiceTier: '#service-tier'
```
```openapi-schema
kind: schema
name: CreateChatCompletionStreamResponse
schema:
  type: object
  description: A single streamed chunk of a chat completion, delivered as a server-sent event.
  properties:
    id:
      type: string
      description: Identifier for this chat completion. Identical across every chunk in the stream.
    choices:
      type: array
      description: List of completion choices. Contains a single choice, and is empty on the final chunk when `stream_options.include_usage` is set.
      items:
        type: object
        required:
        - delta
        - finish_reason
        - index
        properties:
          delta:
            $ref: '#/components/schemas/ChatCompletionStreamResponseDelta'
          logprobs:
            description: Log-probability data for this choice.
            type: object
            nullable: true
            properties:
              content:
                description: Content tokens with their log probabilities.
                type: array
                items:
                  $ref: '#/components/schemas/ChatCompletionTokenLogprob'
                nullable: true
              refusal:
                description: Refusal tokens with their log probabilities.
                type: array
                items:
                  $ref: '#/components/schemas/ChatCompletionTokenLogprob'
                nullable: true
            required:
            - content
            - refusal
          finish_reason:
            type: string
            description: 'Why the model stopped generating tokens: `stop` for a natural end, `length` when the output-token limit was reached, and `tool_calls` when the model requested a tool. `content_filter` and the deprecated `function_call` may also appear.'
            enum:
            - stop
            - length
            - tool_calls
            - content_filter
            - function_call
            nullable: true
          index:
            type: integer
            description: Index of this choice in the `choices` array.
    created:
      type: integer
      format: unixtime
      description: Unix timestamp, in seconds, when the chat completion was created. Identical across every chunk in the stream.
    model:
      type: string
      description: ID of the model that generated the completion.
    service_tier:
      $ref: '#/components/schemas/ServiceTier'
    system_fingerprint:
      type: string
      deprecated: true
      description: Identifies the backend configuration the model ran with, for use with `seed` to detect determinism-affecting changes. Not currently populated.
    object:
      type: string
      description: Object type; always `chat.completion.chunk`.
      enum:
      - chat.completion.chunk
    usage:
      $ref: '#/components/schemas/CompletionUsage'
      nullable: true
      description: Token usage for the entire request. Present only when `stream_options.include_usage` is set, and populated only on the final chunk; null on all earlier chunks. An interrupted or cancelled stream may omit this chunk.
  required:
  - choices
  - created
  - id
  - model
  - object
components:
  schemas: {}
page_linked_schemas:
  ChatCompletionMessageToolCallChunk: '#chat-completion-message-tool-call-chunk'
  ChatCompletionStreamResponseDelta: '#chat-completion-stream-response-delta'
  ChatCompletionTokenLogprob: '#chat-completion-token-logprob'
  CompletionUsage: '#completion-usage'
  CreateChatCompletionStreamResponse: '#create-chat-completion-stream-response'
  ServiceTier: '#service-tier'
```
```openapi-schema
kind: schema
name: CreateModelResponseProperties
schema:
  allOf:
  - $ref: '#/components/schemas/ModelResponseProperties'
  - type: object
    properties:
      top_logprobs:
        description: An integer between 0 and 20 giving the number of top alternative tokens to return at each position, each with its log probability.
        type: integer
        minimum: 0
        maximum: 20
components:
  schemas: {}
page_linked_schemas:
  CreateModelResponseProperties: '#create-model-response-properties'
  Metadata: '#metadata'
  ModelResponseProperties: '#model-response-properties'
  ServiceTier: '#service-tier'
```
```openapi-schema
kind: schema
name: FunctionObject
schema:
  type: object
  properties:
    description:
      type: string
      description: Tells the model what the function does, so it can decide when and how to call it.
    name:
      type: string
      description: Name of the function to call. May contain letters, digits, underscores, dashes, and at most one dot.
    parameters:
      $ref: '#/components/schemas/FunctionParameters'
    strict:
      anyOf:
      - type: boolean
        default: false
        description: When `true`, the model must produce arguments that match the `parameters` schema exactly. Only a subset of JSON Schema is supported in this mode. Defaults to `false`.
      - type: 'null'
  required:
  - name
components:
  schemas: {}
page_linked_schemas:
  FunctionObject: '#function-object'
  FunctionParameters: '#function-parameters'
```
```openapi-schema
kind: schema
name: FunctionParameters
schema:
  type: object
  description: The parameters the functions accepts, described as a JSON Schema object. See the [JSON Schema reference](https://json-schema.org/understanding-json-schema/) for documentation about the format. Omitting `parameters` defines a function with an empty parameter list.
  additionalProperties: true
components:
  schemas: {}
page_linked_schemas:
  FunctionParameters: '#function-parameters'
```
```openapi-schema
kind: schema
name: Metadata
schema:
  anyOf:
  - type: object
    description: Up to 16 key-value pairs of additional metadata to attach to the object. Keys are strings up to 64 characters; values are strings up to 512 characters.
    additionalProperties:
      type: string
  - type: 'null'
components:
  schemas: {}
page_linked_schemas:
  Metadata: '#metadata'
```
```openapi-schema
kind: schema
name: ModelResponseProperties
schema:
  type: object
  properties:
    metadata:
      $ref: '#/components/schemas/Metadata'
    top_logprobs:
      anyOf:
      - description: 'An integer between 0 and 20 specifying the number of most likely tokens to

          return at each token position, each with an associated log probability.

          '
        type: integer
        minimum: 0
        maximum: 20
      - type: 'null'
    temperature:
      anyOf:
      - type: number
        example: 1
        minimum: 0
        maximum: 2
        default: 1
        description: 'What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.

          We generally recommend altering this or `top_p` but not both.

          '
      - type: 'null'
    top_p:
      anyOf:
      - type: number
        example: 1
        minimum: 0
        maximum: 1
        default: 1
        description: 'Nucleus sampling: only tokens whose cumulative probability is at most this value are considered. Must be greater than 0; values <= 0 return HTTP 400. Set this or `temperature`, not both.'
      - type: 'null'
    user:
      type: string
      example: user_9f3a2c1d
      deprecated: true
      description: Being replaced by `safety_identifier` and `prompt_cache_key`; use `prompt_cache_key` to retain caching benefits. A stable end-user identifier used to improve cache hit rates and help detect abuse.
    safety_identifier:
      type: string
      example: safety_9f3a2c1d
      maxLength: 64
      description: A stable identifier that helps detect users who may be violating usage policies. Use a string that uniquely identifies each user, up to 64 characters; hash a username or email so you do not send identifying information.
    prompt_cache_key:
      type: string
      example: prompt_cache_9f3a2c1d
      description: Groups similar requests to improve cache hit rates. Replaces the `user` field.
    service_tier:
      $ref: '#/components/schemas/ServiceTier'
    prompt_cache_retention:
      anyOf:
      - type: string
        enum:
        - in_memory
        - 24h
        description: The retention policy for the prompt cache.
      - type: 'null'
components:
  schemas: {}
page_linked_schemas:
  Metadata: '#metadata'
  ModelResponseProperties: '#model-response-properties'
  ServiceTier: '#service-tier'
```
```openapi-schema
kind: schema
name: ParallelToolCalls
schema:
  description: Whether to enable parallel function calling during tool use.
  type: boolean
  default: true
components:
  schemas: {}
page_linked_schemas:
  ParallelToolCalls: '#parallel-tool-calls'
```
```openapi-schema
kind: schema
name: ReasoningEffort
schema:
  anyOf:
  - type: string
    enum:
    - none
    - minimal
    - low
    - medium
    - high
    - xhigh
    default: medium
    description: 'Constrains effort on reasoning. Accepted values: `none`, `minimal`, `low`, `medium`, `high`, `xhigh`; `none` disables reasoning. Reducing reasoning effort can result in faster responses and fewer tokens used on reasoning in a response.'
  - type: 'null'
components:
  schemas: {}
page_linked_schemas:
  ReasoningEffort: '#reasoning-effort'
```
```openapi-schema
kind: schema
name: ResponseFormatJsonObject
schema:
  type: object
  title: JSON object
  description: 'JSON mode: forces the output to be valid JSON. Prefer `json_schema` where supported. The model only emits JSON if a system or user message instructs it to.'
  properties:
    type:
      type: string
      description: Response-format type; always `json_object`.
      enum:
      - json_object
  required:
  - type
components:
  schemas: {}
page_linked_schemas:
  ResponseFormatJsonObject: '#response-format-json-object'
```
```openapi-schema
kind: schema
name: ResponseFormatJsonSchema
schema:
  type: object
  title: JSON schema
  description: JSON Schema response format. Used to generate structured JSON responses.
  properties:
    type:
      type: string
      description: Response-format type; always `json_schema`.
      enum:
      - json_schema
    json_schema:
      type: object
      title: JSON schema
      description: Structured Outputs settings, including the JSON Schema to enforce.
      properties:
        description:
          type: string
          description: Describes what the format is for; the model uses it to decide how to respond.
        name:
          type: string
          description: 'Name of the response format. Allowed characters: a-z, A-Z, 0-9, underscores, and dashes, up to 64 characters.'
        schema:
          $ref: '#/components/schemas/ResponseFormatJsonSchemaSchema'
        strict:
          anyOf:
          - type: boolean
            default: false
            description: When true, the model's output strictly conforms to the JSON Schema defined in the `schema` field. Only a subset of JSON Schema is supported in strict mode.
          - type: 'null'
      required:
      - name
  required:
  - type
  - json_schema
components:
  schemas: {}
page_linked_schemas:
  ResponseFormatJsonSchema: '#response-format-json-schema'
  ResponseFormatJsonSchemaSchema: '#response-format-json-schema-schema'
```
```openapi-schema
kind: schema
name: ResponseFormatJsonSchemaSchema
schema:
  type: object
  title: JSON schema
  description: The response-format schema, expressed as a JSON Schema object. See the [JSON Schema reference](https://json-schema.org/) for the format.
  additionalProperties: true
components:
  schemas: {}
page_linked_schemas:
  ResponseFormatJsonSchemaSchema: '#response-format-json-schema-schema'
```
```openapi-schema
kind: schema
name: ResponseFormatText
schema:
  type: object
  title: Text
  description: Plain-text response format (the default).
  properties:
    type:
      type: string
      description: Response-format type; always `text`.
      enum:
      - text
  required:
  - type
components:
  schemas: {}
page_linked_schemas:
  ResponseFormatText: '#response-format-text'
```
```openapi-schema
kind: schema
name: ResponseModalities
schema:
  anyOf:
  - type: array
    description: Output types to generate; currently only `text` is supported.
    items:
      type: string
      enum:
      - text
      - audio
  - type: 'null'
components:
  schemas: {}
page_linked_schemas:
  ResponseModalities: '#response-modalities'
```
```openapi-schema
kind: schema
name: ServiceTier
schema:
  anyOf:
  - type: string
    description: Service tier under which the request was processed.
    enum:
    - auto
    - default
    - flex
    - scale
    - priority
    default: auto
  - type: 'null'
components:
  schemas: {}
page_linked_schemas:
  ServiceTier: '#service-tier'
```
```openapi-schema
kind: schema
name: StopConfiguration
schema:
  description: Not supported with reasoning models. Up to 4 sequences where the API will stop generating further tokens. The returned text will not contain the stop sequence.
  default: null
  nullable: true
  oneOf:
  - type: string
    example: '

      '
    default: <|endoftext|>
    nullable: true
  - type: array
    minItems: 1
    maxItems: 4
    items:
      type: string
      example: '

        '
components:
  schemas: {}
page_linked_schemas:
  StopConfiguration: '#stop-configuration'
```